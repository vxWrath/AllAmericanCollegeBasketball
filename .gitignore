# Environment files — only .env.example is committed
.env
.env.dev
.env.qa
.env.prod

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
dist/
build/
*.egg-info/

# Alembic — version files ARE committed so CI can run migrations
migrations/versions/.gitkeep
